version: "3.7"
services:
  morphicweb:
    build:
      context: .
    ports:
      - "5003:80"
    environment:
      SERVICE_URL:    "${SERVICE_URL:-http://localhost:5002/}"
      RECAPTCHA_KEY:  "${RECAPTCHA_KEY:-6Le6hfsUAAAAAB9-WagMKxjnZmP7RKrSxlTYjJc-}"
    volumes:
    - type: bind
      source: ./www
      target: /MorphicLiteWeb/www

